{% extends "base.html.twig" %}

{% block title %}{{ 'Edit Pokemon'|trans }}{% endblock %}

{% block content %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header">
                    <h3 class="card-title mb-0">{{ 'Edit Pokemon'|trans }}</h3>
                </div>
                <div class="card-body">
                    {% if error %}
                        <div class="alert alert-danger">{{ error }}</div>
                    {% endif %}

                    <form method="POST" action="/admin/pokemon/edit/{{ pokemon.Number }}">
                        <div class="mb-3">
                            <label for="number" class="form-label">{{ 'Number'|trans }}</label>
                            <input type="number" class="form-control" id="number" value="{{ pokemon.Number }}" disabled>
                            <small class="text-muted">{{ 'Pokemon number cannot be changed'|trans }}</small>
                        </div>

                        <div class="mb-3">
                            <label for="name" class="form-label">{{ 'Name'|trans }}</label>
                            <input type="text" class="form-control" id="name" name="name" value="{{ pokemon.Name }}" required>
                        </div>

                        <div class="mb-3">
                            <label for="imagePath" class="form-label">{{ 'Image Path'|trans }}</label>
                            <input type="text" class="form-control" id="imagePath" name="imagePath" value="{{ pokemon.ImagePath }}" required>
                            <small class="text-muted">{{ 'Enter the filename of the image (e.g., bulbasaur.png)'|trans }}</small>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a href="/admin/dashboard" class="btn btn-secondary">
                                <i class="fas fa-arrow-left"></i> {{ 'Back to Dashboard'|trans }}
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-save"></i> {{ 'Save Changes'|trans }}
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %} 